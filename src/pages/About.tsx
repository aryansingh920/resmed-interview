export default function About() {
  return (
    <div className="page">
      <h2>About</h2>
      <p>CRA + TypeScript + React Context + Chart.js (via react-chartjs-2).</p>
      <p>
        Data is stored locally (localStorage) and can be imported/exported as
        JSON or CSV.
      </p>
      <p>
        Built by <strong>Aryan Singh</strong> as part of the{" "}
        <strong>ResMed interview project</strong>.
      </p>
    </div>
  );
}
